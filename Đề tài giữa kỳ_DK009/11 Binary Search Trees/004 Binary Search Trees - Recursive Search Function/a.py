def recursive_search(t_root, key):
    # Mã nguồn Python sẽ được thêm sau


if t_root:
    if key == t_root.element:
        return True
    elif key < t_root.element:
        # Tìm kiếm đệ quy ở cây con bên trái
    else:
        # Tìm kiếm đệ quy ở cây con bên phải
else:
    return False  # Nếu t_root là None, trả về False
