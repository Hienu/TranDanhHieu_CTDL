def shellsort(a):
    n = len(a)
    gap = n // 2
    while gap > 0:
        # Các bước của thuật toán
        gap //= 2

def shellsort(a):
    n = len(a)
    # Các bước tiếp theo của hàm

def shellsort(a):
    n = len(a)
    gap = n // 2
    while gap > 0:
        # Các bước của thuật toán
        gap //= 2

def shellsort(a):
    n = len(a)
    gap = n // 2
    while gap > 0:
        # Các bước của thuật toán
        gap //= 2

