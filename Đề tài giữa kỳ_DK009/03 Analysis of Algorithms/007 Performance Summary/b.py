# Ví dụ với Big O
def example_big_o_worst_case(input_list):
    sorted_list = sorted(input_list)  # O(N log N)
    return sorted_list

# (Không có ví dụ cho Big Omega vì nó thường không được sử dụng cho trường hợp xấu nhất)

# (Không có ví dụ cho Big Theta vì thường không được sử dụng cho trường hợp xấu nhất)
