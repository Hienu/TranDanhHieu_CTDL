stack_data = [1, 2, 3, 4, 5]
stack_data.append(6)  # Thêm phần tử vào Stack
popped_item = stack_data.pop()  # Lấy phần tử ra khỏi Stack
top_item = stack_data[-1]  # Xem phần tử đầu tiên mà không xóa
is_empty = len(stack_data) == 0  # Kiểm tra xem Stack có trống không
